# Python dependencies, pinned to the versions the results were produced with.
# Python 3.13.5.
#
#   pip install -r requirements.txt
#
# Reproducing the figures needs only the first block. The rest is for re-running
# the simulation pipeline, which additionally requires a cluster (see README).

# --- Required to reproduce the figures ------------------------------------
numpy==2.1.3
pandas==2.2.3
matplotlib==3.10.0
scipy==1.15.3                # the 1D true densities are evaluated at figure time

# --- Required to re-run the simulation pipeline ---------------------------
scikit-learn==1.6.1          # KDE and DPM baselines
joblib==1.4.2                # parallelism in the tree recursions
numba==0.61.0                # njit kernels in src/core/OPT_nD_functions.py
seaborn==0.13.2
statsmodels==0.14.4
tabulate==0.9.0

# --- Required only for the flow-cytometry figure --------------------------
# That data is not distributed with this repository; see README.
pyreadr==0.5.6               # reads the .Rdata flow file
